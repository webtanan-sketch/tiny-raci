import{describe,expect,it}from'vitest';import{TinyRaciRepository}from'./repository';class S{d=new Map<string,unknown>();async get<T>(k:string){return(this.d.get(k)as T|undefined)??null}async set<T>(k:string,v:T){this.d.set(k,v)}}describe('raci',()=>{it('creates minimal RACI entry',async()=>{const r=await new TinyRaciRepository(new S()).create({activity:'Approve budget',responsible:['Ali'],accountable:'Sara'});expect(r.responsible).toEqual(['Ali']);expect(r.accountable).toBe('Sara')})});
